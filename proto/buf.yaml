version: v2
modules:
  - path: .
    name: buf.build/wcygan/simple-connect-web-stack
breaking:
  use:
    - FILE
lint:
  use:
    - DEFAULT
  except:
    - PACKAGE_VERSION_SUFFIX
  ignore_only:
    ENUM_ZERO_VALUE_SUFFIX:
      - proto/todo/v1/todo.proto